package school.hei.asa.model;

public final class PartnerContractor extends Contractor {
  public PartnerContractor(
      String code,
      String name,
      String email,
      String fullname,
      String address,
      String city,
      String nif,
      String stat) {
    super(code, name, email, fullname, address, city, nif, stat);
  }
}
